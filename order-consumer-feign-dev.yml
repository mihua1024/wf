server:
  port: 9003
eureka:
  client:
    service-url:
      defaultZone: http://root:ok@localhost:7001/eureka/
  instance:
    #在状态栏可见ip
    prefer-ip-address: true

feign:
  hystrix:
    enabled: true
name: WE

